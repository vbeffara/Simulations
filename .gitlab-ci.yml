image: ubuntu:cosmic

before_script:
  - apt-get update -qq
  - apt-get install -qq python3-pip cmake g++ ninja-build git lcov ruby libgl1-mesa-dev libxcb-shm0
  - apt-get install -qq xvfb libglu1-mesa-dev xorg-dev libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev

cmake:
  stage: build
  script:
    - pip3 install -q conan
    - conan remote add bincrafters https://api.bintray.com/conan/bincrafters/public-conan
    - conan remote add vb https://api.bintray.com/conan/vbeffara/misc
    - mkdir -p build/cov
    - cd build
    - conan install .. --build missing -s compiler.libcxx=libstdc++11
    - cmake .. -G Ninja -DCOV=1
    - ninja
  artifacts:
    paths:
      - build/

unittests:
  stage: test
  script:
    - gem install coveralls-lcov
    - pip3 install codecov
    - Xvfb :4 &
    - export DISPLAY=:4
    - lcov -z -d build
    - export LD_LIBRARY_PATH=./build/lib
    - ./build/tests/doctest
    - lcov -c -d build -d libvb -o build/cov/cov.info --no-external -q
    - codecov -t $CODECOV_TOKEN
    - coveralls-lcov -t $COVERALLS_TOKEN build/cov/cov.info
