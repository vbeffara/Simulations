
def build (bld) :
    bld.new_task_gen(features = 'cxx cshlib',
            source = bld.glob('*.cpp'),
            target = 'vb',
            includes='.',
            uselib='fltk png')

# vim: ft=python
