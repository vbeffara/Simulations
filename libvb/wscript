
def build (bld) :
    bld.new_task_gen(features = 'cxx cshlib',
            source = bld.glob('*.cpp'),
            target = 'vb',
            includes = '.',
            export_incdirs = '.',
            uselib='fltk png')

# vim: ft=python
